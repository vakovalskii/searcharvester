fastapi==0.137.2
fastmcp==3.4.2
uvicorn[standard]==0.35.0
aiohttp==3.9.1
pydantic==2.11.7
beautifulsoup4==4.12.2
pyyaml==6.0.1
trafilatura>=1.12,<3.0
lxml>=4.9.0
docker==7.1.0
sse-starlette==2.1.3

# Test-only (baked into image so we can `docker compose exec tavily-adapter pytest`
# without a re-install on every run).
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.28.1
